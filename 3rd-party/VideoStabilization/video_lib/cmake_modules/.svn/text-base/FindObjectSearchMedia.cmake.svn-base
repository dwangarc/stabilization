FIND_PACKAGE(Boost 1.41 REQUIRED system)
FIND_PACKAGE(OpenCV REQUIRED)
FIND_PACKAGE(Xml REQUIRED)
FIND_PACKAGE(Config REQUIRED)
FIND_PACKAGE(Logger REQUIRED)


# Include directories for project
GET_FILENAME_COMPONENT(OBJECT_SEARCH_MEDIA_CMAKE_CURRENT_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
SET(OBJECT_SEARCH_MEDIA_INCLUDE_DIR
    ${Boost_INCLUDE_DIRS}
    ${OpenCV_INCLUDE_DIR}
    ${XML_INCLUDE_DIR}
    ${CONFIG_INCLUDE_DIR}
    ${LOGGER_INCLUDE_DIR}
	${OBJECT_SEARCH_MEDIA_CMAKE_CURRENT_DIR}/../include
)

# Project library name
SET(OBJECT_SEARCH_MEDIA_LIBRARY object_search_media)

# Project dependenses
SET(OBJECT_SEARCH_MEDIA_LIBRARIES
	${OBJECT_SEARCH_MEDIA_LIBRARY}
	${Boost_LIBRARIES}
	${OpenCV_LIBS}
	${XML_LIBRARIES}
	${CONFIG_LIBRARIES}
	${LOGGER_LIBRARIES}
)

# Result flag
SET(OBJECT_SEARCH_MEDIA_FOUND TRUE)

# Project specific global definitions
SET(OBJECT_SEARCH_MEDIA_DEFINITIONS
    ${XML_DEFINITIONS}
    ${CONFIG_DEFINITIONS}
    ${LOGGER_DEFINITIONS}

)
